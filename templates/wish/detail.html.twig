{% extends 'base.html.twig' %}


{% block title %}Détail wish{% endblock %}

{% block body %}

    <div class="row">

        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{ wish.title|capitalize }}</h5>
                <p class="card-text">{{ wish.description }}</p>
                <p>{{ wish.category.name }}</p>
                <small>{{ wish.author }} | {{ wish.dateCreated|date("d-m-Y H:i") }}</small>
            </div>
        </div>
    </div>
    <div class="row mt-5">
    </div>
    <a class="btn btn-primary" href="{{ path("app_wish_list") }}" role="button">Retour Liste</a>

{% endblock %}